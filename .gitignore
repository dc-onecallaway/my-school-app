# Dependencies (We install these on the server, don't upload them)
node_modules/
*/node_modules/

# Environment Variables (Keep your passwords secret!)
.env
.env.local
.env.*
server/.env
client/.env

# Build Outputs (Netlify/Render will build these themselves)
dist/
build/
client/dist/

# Logs and OS files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db